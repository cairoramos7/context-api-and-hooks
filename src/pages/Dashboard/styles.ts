import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
    },
    containerUser: {
        flex: 0,
        alignItems: 'center',
        marginBottom: 10,
    },
    defaultText: {
        fontSize: 25,
    },
});
