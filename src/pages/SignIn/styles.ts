import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
    },
    textContainer: {
        flex: 0,
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 15,
    },
    defaultText: {
        fontSize: 30,
        fontWeight: 'bold',
    }
});
